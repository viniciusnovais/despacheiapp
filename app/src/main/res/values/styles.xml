<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>


    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />[

    <style name="EditTextTheme">
        <!--<item name="colorControlNormal">#ff0000</item>-->
        <item name="colorControlActivated">#006aff</item>
        <!--<item name="colorControlHighlight">@color/accent_material_light</item>-->
    </style>

    <style name="ToolbarTitle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">

        <item name="android:textColorHint">@color/colorPrincipal</item>

        <item name="colorAccent">@color/colorPrincipal</item>
        <item name="colorControlNormal">@color/colorPrincipal</item>
        <item name="colorControlActivated">@color/colorPrincipal</item>

    </style>
</resources>
